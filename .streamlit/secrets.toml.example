# Copy this setting to Streamlit Community Cloud > Advanced settings > Secrets.
# Generate the value with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
BROWSER_API_COOKIE_KEY = "replace-with-a-private-fernet-key"
