import { catalog, publicSeries } from "./_catalog.js";
const iso=(value,fallback)=>/^\d{4}-\d{2}-\d{2}$/.test(String(value||""))?String(value):fallback;
function monthly(points){const groups=new Map();for(const point of points){const month=point.date.slice(0,7),values=groups.get(month)||[];values.push(point.value);groups.set(month,values);}return[...groups].map(([date,values])=>({date,value:Number((values.reduce((a,b)=>a+b,0)/values.length).toFixed(4))}));}
async function fetchFred(providerId,start,end){const key=process.env.FRED_API_KEY;if(key){const url=new URL("https://api.stlouisfed.org/fred/series/observations");url.searchParams.set("series_id",providerId);url.searchParams.set("api_key",key);url.searchParams.set("file_type","json");url.searchParams.set("observation_start",start);url.searchParams.set("observation_end",end);const response=await fetch(url,{headers:{accept:"application/json"}});if(response.ok){const payload=await response.json();const points=(payload.observations||[]).filter((row)=>row.value!=="."&&Number.isFinite(Number(row.value))).map((row)=>({date:row.date,value:Number(row.value)}));if(points.length)return points;}}const url=new URL("https://fred.stlouisfed.org/graph/fredgraph.csv");url.searchParams.set("id",providerId);url.searchParams.set("cosd",start);url.searchParams.set("coed",end);const response=await fetch(url);if(!response.ok)throw new Error(`FRED CSV ${response.status}`);return(await response.text()).trim().split(/\r?\n/).slice(1).flatMap((line)=>{const[date,raw]=line.split(","),value=Number(String(raw??"").trim());return date&&Number.isFinite(value)?[{date,value}]:[];});}
function decodeEia(id){return JSON.parse(Buffer.from(id.slice(5),"base64url").toString("utf8"));}
async function fetchEia(id,start,end,frequency){const{route,facet,series}=decodeEia(id),key=process.env.EIA_API_KEY||"DEMO_KEY";const metaUrl=new URL(`https://api.eia.gov/v2/${route}/`);metaUrl.searchParams.set("api_key",key);const metaResponse=await fetch(metaUrl);if(!metaResponse.ok)throw new Error(`EIA metadata ${metaResponse.status}`);const metadata=(await metaResponse.json()).response||{},available=(metadata.frequency||[]).map((row)=>row.id),requested=frequency==="monthly"?"monthly":"daily",chosen=available.includes(requested)?requested:available.includes("monthly")?"monthly":available[0];const url=new URL(`https://api.eia.gov/v2/${route}/data/`);url.searchParams.set("api_key",key);if(chosen)url.searchParams.set("frequency",chosen);url.searchParams.set("data[0]","value");url.searchParams.set(`facets[${facet}][]`,series);url.searchParams.set("start",start);url.searchParams.set("end",end);url.searchParams.set("sort[0][column]","period");url.searchParams.set("sort[0][direction]","asc");url.searchParams.set("length","5000");const response=await fetch(url,{headers:{accept:"application/json"}});if(!response.ok)throw new Error(`EIA data ${response.status}`);const payload=await response.json();if(payload.error)throw new Error(payload.error.message||"EIA API error");const rows=payload.response?.data||[],points=rows.flatMap((row)=>Number.isFinite(Number(row.value))?[{date:String(row.period),value:Number(row.value)}]:[]);if(!points.length)throw new Error("The selected EIA series has no observations in this date range");return{points:requested==="monthly"&&chosen!=="monthly"?monthly(points):points,name:rows[0]?.[`${facet}-name`]||rows[0]?.["series-description"]||series,unit:rows[0]?.units||"",frequency:chosen||requested};}
export default async function handler(request,response){const id=String(request.query?.id||""),today=new Date().toISOString().slice(0,10),start=iso(request.query?.start,"2000-01-01"),end=iso(request.query?.end,today),frequency=request.query?.frequency==="monthly"?"monthly":"daily";try{if(id.startsWith("EIA2-")){const result=await fetchEia(id,start,end,frequency);response.setHeader("Cache-Control","public, s-maxage=1800, stale-while-revalidate=43200");return response.status(200).json({id,name:result.name,nameEn:result.name,source:"EIA",unit:result.unit,frequency:result.frequency,updated:result.points.at(-1)?.date||"",color:"#9b6d51",points:result.points});}const item=catalog.find((candidate)=>candidate.id===id)||(/^FRED-[A-Z0-9_]+$/i.test(id)?{id,providerId:id.slice(5),name:id.slice(5),nameEn:id.slice(5),source:"FRED",unit:"",frequency:"",color:"#587a9a"}:null);if(!item)return response.status(404).json({error:"Unknown series"});const raw=await fetchFred(item.providerId,start,end),points=frequency==="monthly"?monthly(raw):raw;response.setHeader("Cache-Control","public, s-maxage=1800, stale-while-revalidate=43200");return response.status(200).json(publicSeries(item,points));}catch(error){return response.status(502).json({error:"Official data provider unavailable",detail:String(error?.message||error)});}}
